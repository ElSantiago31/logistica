# FastAPI
fastapi==0.111.0
uvicorn[standard]==0.30.1
# WebSocket support (tiempo real: check-in/intendencia/nómina)
websockets==12.0
wsproto==1.2.0

# Database
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.1

# Auth & Security
# python-jose 3.3.0 reemplazado por PyJWT (jose está abandonado, CVE-2024-33663 DoS)
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# Settings
pydantic-settings==2.3.4
python-dotenv==1.0.1

# HTTP Client (WhatsApp)
httpx==0.27.0

# Image Processing (CVE-2025-48379 parchado en 11.3.0)
Pillow==11.3.0

# Rate Limiting
slowapi==0.1.9

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# Utilities (CVE-2024-53981 DoS parchado en 0.0.18+)
python-multipart==0.0.20

# Excel generation (planilla de pago por coordinador)
openpyxl==3.1.5

# PDF generation (facturas individuales y masivas)
reportlab==4.1.0

# PDF compression (RUT de operadores)
PyMuPDF==1.24.7

# Email sender (SMTP async — respuestas PQRSF)
aiosmtplib==3.0.2
email-validator==2.2.0
